'use strict';

/**
 * contact-form-submission service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::contact-form-submission.contact-form-submission');
